<%-include('../partials/user-header')%> 

    <section class="signin-page account">
        <div class="container">
            <div class="row">
                <div class="col-md-6 col-md-offset-3">
                    <div class="block text-center">
                        <a class="logo" href="index.html">
                            <img src="images/logo.png" alt="">
                        </a>
                        <h2 class="text-center">Otp Verification</h2>
                        <form class="text-left clearfix" action="/forgetOtp" method="post">
                            <div class="form-group">
                                <input type="tel" name="phone" class="form-control" placeholder="Phone number">
                            </div>
                            <div class="form-group">
                                <button type="submit" , class="btn btn-main text-center">Send OTP</button>
                            </div>

                        </form>
                        <% if(locals.msg) { %>
                            <div id="msg" class="alert alert-success" style="color: black;">
                                <%- locals.msg%>
                            </div>
                            <% } %>

                                <!-- <form class="text-left clearfix" action="/verifyOtp" method="post"
                                    novalidate="novalidate">
                                    <div class="form-group">
                                        <input type="tel" id="otp" name="otp" class="form-control"
                                            placeholder="Enter OTP">
                                    </div>
                                    

                                    <div class="text-center">
                                        <button type="submit" value="submit"
                                            class="btn btn-main text-center">Login</button>
                                    </div>
                                </form> -->
                    </div>
                </div>
            </div>
        </div>
    </section>

        <script>
            setTimeout(() => {
                document.getElementById('msg').style.display = 'none'
            }, 2000)
        </script>


<%-include('../partials/user-footer')%> 